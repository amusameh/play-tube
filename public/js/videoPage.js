//read select from app.js
const commentReply = document.getElementsByClassName('comment-reply');
const subCommentDiv = select('.sub-Comment-input');

/*
commentReply.addEventListener('click',(e)=>{
  console.log(1);
  // subCommentDiv.classList.toggle('hiddenComment');
})

Array.from(commentReply).forEach(element => {
      element.addEventListener('click',(e)=>{
        console.log(1);

        // console.log(subCommentDiv.parentElement.nextElementSibling.innerHTML)
        // console.log(e.target.parentElement.parentElement.nextElementSibling);
        // e.target.parentElement.parentElement.nextElementSibling.classList.toggle('hiddenComment');
      });
});
*/
// <div class="sub-Comment-input " id="subComment">
//   <input type="text" name="subComment" value="">
// </div>

// main-comment >> height 122 > 75
// inp.sub-Comment-inputut >>   margin-top: 31px; to 5
